%h1= @product.name
%p= number_to_currency @product.price
%p
  = image_tag @product.image, height: "300"

%p
  = link_to 'Add to cart', add_cart_path, class: "btn btn-primary", method: :post
= link_to 'Edit', edit_product_path(@product)
\|
= link_to 'Back', products_path
